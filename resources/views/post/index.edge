@layout('layouts.main')

@section('content')
    <div class="container">
        @set('pageTitle', 'Posts')
        
        @!component('components.post.list', {
            items:data
        })

        @!component('components.pagination', {
            total,
            perPage,
            page,
            lastPage
        })
    </div>
@endsection
